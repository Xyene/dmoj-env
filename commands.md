# Vagrant commands

- Creating the VM

```sh
vagrant up --provider virtualbox
```

- Update shutdown VM

```sh
vagrant up --provision
```

- Restart running VM

```sh
vagrant reload --provision
```

- Delete VM

```sh
vagrant destroy
```

- Connect to VM

```sh
vagrant ssh
```
